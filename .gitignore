# dependencies
node_modules

# build output
dist
dist-ssr
docs/.vitepress/dist
docs/.vitepress/cache

# test / lint caches
coverage
.eslintcache
*.tsbuildinfo

# local env + secrets
.env
.env.*
!.env.example

# editor / OS / local tool settings
*.local
.vite
.DS_Store
.claude/

# internal-only notes (kept locally, never published)
REVIEW.md
docs/CODE_REVIEW.md
